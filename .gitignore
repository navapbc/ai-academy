node_modules

# Local env (keep .env.example committed)
.env
.env.local
.env.*.local

# Edge Function secrets (ANTHROPIC_API_KEY) — keep .env.example committed.
# Covered by the bare `.env` rule above, but listed explicitly to be safe.
supabase/functions/.env

dist

# Playwright E2E artifacts
test-results
playwright-report
.playwright

# Supabase local temp artifacts
supabase/.branches
supabase/.temp

.vscode
pnpm-lock.yaml
