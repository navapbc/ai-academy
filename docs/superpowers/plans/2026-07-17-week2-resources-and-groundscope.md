# Week 2 Resources + Ground/Scope Copy Tighten Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add two public resource lessons (connectors; Claude Projects) so the Week 2 live-session deck's AI Academy pointers resolve, and tighten the "Ground & Scope for Improvement" activity copy so it honestly frames itself as a grounding contrast.

**Architecture:** Pure content-as-data change. All edits go into the source-of-truth JSON (`supabase/seed-data/course1-content.json`); the migration SQL is regenerated by a Node script and never hand-edited. No app code, types, or components change. Stacks on the Week 1 resources + sorter branch.

**Tech Stack:** JSON seed data, Node ESM generator (`scripts/generate-course1-seed.mjs`), Postgres migration (Supabase), Vitest, TypeScript.

## Global Constraints

- **Never hand-edit** `supabase/migrations/20260715040000_seed_course1_content.sql`. Change the JSON, then run `node scripts/generate-course1-seed.mjs`.
- **Node 22 required** for `npm run lint` / `npm test`. Activate by sourcing nvm in the SAME shell command: `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 22 >/dev/null; node -v && npm run lint && npm test`. Confirm `node -v` prints v22.x; if it prints v20.x, the jsdom tests will wrongly error with `ERR_REQUIRE_ESM` — fix activation before trusting results. Full-suite baseline: **888 passed / 78 skipped / 0 failed**.
- **Branch:** work on `feat/week2-resources-and-groundscope` (already created off `feat/week1-resources-and-sorter`). Do not commit to `main` or the restructure branch.
- **Generator schema:** `origin: custom` requires `week: null` / `week_sort_order: null`; `visibility: public`; `type: content`; `evidence_type: reflection`; `dimension: []`; `lab_config_json: null`; unique `cell_id`; integer `sort_order`. `body_md` must not contain the literal `$md$`.
- **Pre-reveal rule** applies only to `week0`/`week1`. The new resources are `week: null` and the edited activity is `week2`, so neither triggers it.
- Current custom resources occupy `sort_order` 950/951/952; the new ones take **953** and **954**. The JSON currently has **15 modules**; after Task 1 it has **17** (both new resources are `week: null`, so week-assigned stays **12**).
- **Edits are inside `course1-content.json`, a hand-authored JSON file.** Use the Edit tool for exact string replacement — do NOT re-serialize the whole file (it would reformat every line). Inside JSON string values, newlines appear as the literal two-character sequence `\n`; match them as they appear in the file.

---

### Task 1: Add the two Week 2 resource lessons

Append two `origin: custom` module objects to the `modules` array, immediately after `custom-controlling-claude-tools-permissions` (sort_order 952), and regenerate.

**Files:**
- Modify: `supabase/seed-data/course1-content.json`
- Regenerate: `supabase/migrations/20260715040000_seed_course1_content.sql` (via the generator)

**Interfaces:**
- Consumes: nothing.
- Produces: modules `custom-grounding-with-connectors` and `custom-reusing-context-claude-projects`. Resource C's body links "Controlling what Claude can do: tools & permissions" by name; Resource D's body links "How Claude works: tokens & context windows" by name (both exist on this branch).

- [ ] **Step 1: Add Resource C — Grounding with connectors**

Insert this object into the `modules` array, immediately after the `custom-controlling-claude-tools-permissions` object:

```json
{
  "cell_id": "custom-grounding-with-connectors",
  "week": null,
  "week_sort_order": null,
  "origin": "custom",
  "visibility": "public",
  "title": "Grounding with connectors",
  "type": "content",
  "dimension": [],
  "evidence_type": "reflection",
  "sort_order": 953,
  "body_md": "Grounding means giving Claude curated source material to predict from — the single most effective way to lower the odds of a confident wrong answer. Pasting text into the chat is one way to ground; **connectors** are another, for when the source already lives in a system you use.\n\n## What connectors do\n\nInstead of copying everything into the chat yourself, a connector lets Claude pull from a connected space you've granted access to — for example **Confluence, Slack, or your Google Drive**. On Nava's Claude, connectors let you ground a conversation in real content without hunting it down and pasting it in first.\n\n## Choosing good sources\n\nGrounding only helps if the source is worth grounding on. Aim for sources that are:\n\n- **Safe to share** with AI — check Nava's AI Tool Policy and any program-level restrictions first.\n- **Accurate** — you're anchoring the prediction to this, so a wrong source produces a confidently wrong answer.\n- **Narrow** — point Claude at the specific document or space that matters, not \"everything.\" Aiming it at an entire wiki or drive gives it too much to sift and weakens the grounding.\n\n## Retrieval isn't fact-checking\n\nA connector grounds the prediction — it does not turn Claude into a fact-checker. Claude can still misread a source, pull the wrong passage, or lean on a source that is itself out of date. Retrieval lowers the odds of a bad answer; it doesn't remove the need to verify what matters against the source of truth.\n\nYou also control which tools and connectors Claude may use on its own versus which need your approval — see **Controlling what Claude can do: tools & permissions**.",
  "lab_config_json": null
}
```

- [ ] **Step 2: Add Resource D — Reusing context: Claude Projects**

Insert this object into the `modules` array, immediately after Resource C:

```json
{
  "cell_id": "custom-reusing-context-claude-projects",
  "week": null,
  "week_sort_order": null,
  "origin": "custom",
  "visibility": "public",
  "title": "Reusing context: Claude Projects",
  "type": "content",
  "dimension": [],
  "evidence_type": "reflection",
  "sort_order": 954,
  "body_md": "When the same context comes up again and again — the same source documents, the same standing instructions — you don't have to set it up in every new chat. **Projects** let you save that context once and reuse it.\n\n## What a Project is\n\nA Project is a saved workspace that keeps instructions and reference files attached. Every chat you start inside the Project already has your context, so you're not re-pasting sources or re-explaining what you want each time.\n\n## When to use one\n\nReach for a Project when:\n\n- You do a **recurring task** — the same kind of drafting, review, or analysis on a regular basis.\n- The **same grounding and scoping apply across many chats** — one set of sources and instructions you'd otherwise repeat.\n\n## Grounding and scoping, saved once\n\nA Project is where the two habits come together. Put your curated sources (grounding) and your standing instructions — tone, format, what to avoid (scoping) — into the Project once, and every new chat inside it starts from that footing.\n\n## Still start fresh chats per task\n\nA Project doesn't change how the context window works. Each chat inside it still fills up as it goes, so keep the habit of starting a new chat at each logical breakpoint — you just won't lose your saved sources and instructions when you do. For why that matters, see **How Claude works: tokens & context windows**.",
  "lab_config_json": null
}
```

- [ ] **Step 3: Regenerate the migration**

Run: `node scripts/generate-course1-seed.mjs`
Expected: prints `Wrote …20260715040000_seed_course1_content.sql: 17 modules (12 week-assigned).` and exits 0. A validation throw means a field is wrong (e.g. a `custom` module with a non-null `week`, or `body_md` containing `$md$`) — fix and re-run.

- [ ] **Step 4: Spot-check the generated SQL**

Run: `grep -c "custom-grounding-with-connectors\|custom-reusing-context-claude-projects" supabase/migrations/20260715040000_seed_course1_content.sql`
Expected: `2` (one `insert` per new cell_id; both are unassigned, so absent from the membership block).

- [ ] **Step 5: Lint + test**

Run: `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 22 >/dev/null; node -v && npm run lint && npm test`
Expected: `node -v` prints v22.x; lint passes; suite `888 passed | 78 skipped`.

- [ ] **Step 6: Commit**

```bash
git add supabase/seed-data/course1-content.json supabase/migrations/20260715040000_seed_course1_content.sql
git commit -m "feat(resources): add connectors + Claude Projects lessons

Two public custom resources in the Resources group so the Week 2 live
deck's 'AI Academy' pointers (how connectors work for grounding; the
Projects feature for reusing sources/scoping) resolve.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Tighten the Ground & Scope activity copy

Edit only `body_md` and `reflectionMd` of `c1-w2-ground-and-scope`. Do NOT change `kind`, `title`, `subtitle`, `panes`, `sourceMd`, `suggestedPrompts`, or the `## Source material` block.

**Files:**
- Modify: `supabase/seed-data/course1-content.json` (the `c1-w2-ground-and-scope` module)
- Regenerate: `supabase/migrations/20260715040000_seed_course1_content.sql` (via the generator)

**Interfaces:**
- Consumes: Resource D (`Reusing context: Claude Projects`) from Task 1 — the new body copy references it by name.
- Produces: nothing other tasks depend on.

- [ ] **Step 1: Reframe the `body_md` second paragraph**

In the `c1-w2-ground-and-scope` module's `body_md` (a single JSON string), replace this exact substring:

```
One of the best ways to understand foundational prompting strategies is to test them in realistic scenarios. You'll run the same task two ways and compare the answers: the two Claude chats below get the **same prompt**, but only the second one is given the source material shown here.
```

with:

```
This activity is about the first habit — **grounding**. You'll run the same task two ways and compare the answers: the two Claude chats below get the **same prompt**, but only the second one is given the source material shown here. (Scoping, the companion habit, comes up in the live session — and the *Reusing context: Claude Projects* resource shows how to save grounding and scoping together.)
```

(The surrounding "Part of the Week 2 live session…" first paragraph and the `## Source material` block are unchanged.)

- [ ] **Step 2: Add the verify watch-for to `reflectionMd`**

In the same module's `lab_config_json.reflectionMd` (a single JSON string, with literal `\n` between paragraphs), replace this exact substring:

```
impacted the responses?\n\nIf you have time,
```

with:

```
impacted the responses?\n\n**Keep in mind:** Grounding lowers the odds of a wrong answer — it doesn't remove the need to verify. A grounded answer is still an unverified answer until you check it against the source.\n\nIf you have time,
```

(The em dash `—` and apostrophes are literal UTF-8 characters as they appear elsewhere in the file. This inserts a note between reflection question 3 and the closing "If you have time…" paragraph; the three questions and the closing sentence are otherwise unchanged.)

- [ ] **Step 3: Regenerate the migration**

Run: `node scripts/generate-course1-seed.mjs`
Expected: prints `Wrote …: 17 modules (12 week-assigned).` and exits 0. (Still 17 — no new module in this task.)

- [ ] **Step 4: Confirm only the intended module changed**

Run: `git --no-pager diff supabase/seed-data/course1-content.json`
Expected: the diff touches only the `c1-w2-ground-and-scope` module — the reframed second paragraph and the added watch-for line. No other module, and none of the excluded fields (`title`, `subtitle`, `panes`, `sourceMd`, `suggestedPrompts`, source-material block), appear in the diff.

- [ ] **Step 5: Lint + test**

Run: `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 22 >/dev/null; node -v && npm run lint && npm test`
Expected: `node -v` v22.x; lint passes; suite `888 passed | 78 skipped`.

- [ ] **Step 6: Commit**

```bash
git add supabase/seed-data/course1-content.json supabase/migrations/20260715040000_seed_course1_content.sql
git commit -m "feat(week2): frame Ground & Scope activity as a grounding contrast

chat-compare shares one prompt across panes, so the activity demonstrates
grounding (source vs none), not a grounded-and-scoped prompt pair. Reframe
the body accordingly, cross-link the Projects resource, and add a verify
watch-for to the reflection. Scoping stays in the live teach.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: End-to-end verification

Confirm the migration applies to a real database and the new/changed content renders. Reviewer gate that the whole change works, not just that it generates.

**Files:** none modified (verification only).

**Interfaces:**
- Consumes: the committed JSON + regenerated SQL from Tasks 1–2.
- Produces: nothing.

- [ ] **Step 1: Apply the migration (if Docker is available)**

Run: `npx supabase db reset`
Expected: all migrations + seed apply with no error. If Docker Desktop isn't running, skip and note it was skipped.

- [ ] **Step 2: Verify the resources are seeded**

After the reset, query via psql:
`psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -A -F' | ' -c "select cell_id, visibility, sort_order from public.modules where origin='custom' order by sort_order;"`
Expected: five rows — `custom-ai-support-at-nava` (950), `custom-how-claude-works-tokens` (951), `custom-controlling-claude-tools-permissions` (952), `custom-grounding-with-connectors` (953), `custom-reusing-context-claude-projects` (954), all `public`.

- [ ] **Step 3: Verify in the running app**

With the dev server running (`vite-dev` via preview_start) and signed in as the seeded demo learner: confirm the "Resources & additional lessons" group shows the two new lessons and each renders its Markdown body; and open Week 2 → "Ground & Scope for Improvement" and confirm the reframed intro ("This activity is about the first habit — grounding") and the added "Keep in mind" watch-for render. Capture a screenshot as proof.

- [ ] **Step 4: Final green check**

Run: `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 22 >/dev/null; node -v && npm run lint && npm test`
Expected: v22.x; lint passes; suite green. No commit (verification only) unless Step 3 surfaced a content fix.

---

## Self-Review

**Spec coverage:**
- Two public resources (connectors; Projects) → Task 1. ✅
- Ground & Scope copy tighten (body reframe + reflection watch-for), no mechanic change → Task 2. ✅
- Delegation sort unchanged → no task (correct; deck-side reword is the user's). ✅
- Pipeline JSON → generator → migration, no hand-editing → every task's "Regenerate" step. ✅
- Node 22 lint/test green; migration idempotent → task steps + Task 3. ✅
- Out-of-scope deck edits → not in any task (correct). ✅

**Placeholder scan:** No TBD/TODO/"handle edge cases"; full `body_md` for both resources and exact old/new strings for the Ground & Scope edits are inline. ✅

**Type/name consistency:** cell_ids `custom-grounding-with-connectors` (953) / `custom-reusing-context-claude-projects` (954) and the edited `c1-w2-ground-and-scope` are identical across spec, tasks, and the Task 3 query. Cross-linked titles ("Controlling what Claude can do: tools & permissions", "How Claude works: tokens & context windows", "Reusing context: Claude Projects") match their definitions. Module counts (15 → 17, week-assigned 12) consistent across steps. ✅
