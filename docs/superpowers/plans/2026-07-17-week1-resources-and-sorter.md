# Week 1 Resources + Lookup-or-Predict Sorter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add two public "resources section" lessons and realign the Week 1 "Lookup or Predict?" sorter to the Nava-priority items, so the Week 1 live-session deck's references all resolve.

**Architecture:** Pure content-as-data change. All edits go into the source-of-truth JSON (`supabase/seed-data/course1-content.json`); the migration SQL is regenerated by a Node script and never hand-edited. No app code, types, or components change.

**Tech Stack:** JSON seed data, Node ESM generator (`scripts/generate-course1-seed.mjs`), Postgres migration (Supabase), Vitest, TypeScript.

## Global Constraints

- **Never hand-edit** `supabase/migrations/20260715040000_seed_course1_content.sql`. Change the JSON, then run `node scripts/generate-course1-seed.mjs`.
- **Node 22 required** for `npm run lint` / `npm test` (`nvm use 22` first — Node 20 throws `ERR_REQUIRE_ESM` in jsdom tests).
- **Branch:** work on `feat/week1-resources-and-sorter` (already created off `feat/cohort-program-restructure`). Do not commit to `main` or to the restructure branch directly.
- **Generator schema (validated at generate time):** `origin ∈ {course, custom}`; `custom` requires `week: null` and `week_sort_order: null`; `visibility ∈ {public, program}`; `type ∈ {content, lab, ...}`; `evidence_type` from the allow-list (`reflection` for content); `dimension` an array (may be `[]`); non-empty `title` + `body_md`; integer `sort_order`; unique `cell_id`. `body_md` must not contain the literal `$md$`.
- **Pre-reveal copy rule** applies only to `week0`/`week1` modules (no literal `LLM`). The new resources are `week: null`, so it does not apply to them. The sorter (`c1-w1-lookup-vs-predict`) IS `week1`, so its copy must not contain `LLM` — the planned item text does not.
- The current JSON has **13 modules**; after this plan it has **15**. The generator prints the count on success — use it as a check.

---

### Task 1: Realign the "Lookup or Predict?" sorter items

Swap the sorter's items to the final six: keep `capital`, `offsite`, `summary`, `worldcup`; add `pto` and `medicaid`; drop `mockingbird` and `translate`. Only the `items` array of `c1-w1-lookup-vs-predict.lab_config_json` changes — `kind`, `introMd`, `bucketLabels`, `takeaway` stay as-is.

**Files:**
- Modify: `supabase/seed-data/course1-content.json` (the `items` array inside the module with `"cell_id": "c1-w1-lookup-vs-predict"`)
- Regenerate: `supabase/migrations/20260715040000_seed_course1_content.sql` (via the generator — do not edit by hand)

**Interfaces:**
- Consumes: nothing from other tasks.
- Produces: nothing other tasks depend on.

- [ ] **Step 1: Replace the `items` array**

In `c1-w1-lookup-vs-predict` → `lab_config_json.items`, replace the entire array with exactly:

```json
[
  {
    "id": "capital",
    "prompt": "What's the capital of France?",
    "reveal": "Feels like a fact Claude retrieved — but Claude predicted \"Paris\" because those words follow that question countless times in its training. Same machinery as everything else here."
  },
  {
    "id": "offsite",
    "prompt": "Give me three ideas for a team offsite.",
    "reveal": "Obviously generated on the spot — there's no \"right\" answer to retrieve. But the capital of France worked the exact same way."
  },
  {
    "id": "pto",
    "prompt": "What's our company's PTO policy?",
    "reveal": "Feels like Claude is checking an HR page — but it has no access to Nava's policies unless you give them to it. It predicts a plausible-sounding policy that can be wrong in exactly the ways that matter. High-stakes at Nava."
  },
  {
    "id": "summary",
    "prompt": "Summarize this paragraph I just pasted.",
    "reveal": "It's grounded in the text you gave it, yet Claude still predicts the summary word by word — it isn't copying sentences straight out."
  },
  {
    "id": "medicaid",
    "prompt": "What were the Q3 2025 Medicaid enrollment numbers for New Jersey?",
    "reveal": "Nothing to look up here — Claude predicts plausible-looking numbers that can be entirely fabricated. This is the confident-wrong pattern from Experiment 2, aimed straight at the kind of data we work with."
  },
  {
    "id": "worldcup",
    "prompt": "Who won the 2043 World Cup?",
    "reveal": "There's nothing to look up — the match hasn't happened. Claude predicts a plausible-sounding answer anyway. That's how the confident wrong answers in Experiment 2 happen."
  }
]
```

- [ ] **Step 2: Regenerate the migration**

Run: `node scripts/generate-course1-seed.mjs`
Expected: prints `Wrote …20260715040000_seed_course1_content.sql: 13 modules (12 week-assigned).` and exits 0. (Still 13 modules — no new module yet.) If it throws a validation error, the JSON edit is malformed — fix and re-run.

- [ ] **Step 3: Confirm the SQL regenerated and the pre-reveal rule held**

Run: `git --no-pager diff --stat supabase/migrations/20260715040000_seed_course1_content.sql`
Expected: the file shows changes. The generator would have thrown on Step 2 if any `week1` copy contained `LLM`, so a clean run confirms the rule held.

- [ ] **Step 4: Lint + test**

Run: `nvm use 22 >/dev/null; npm run lint && npm test`
Expected: lint passes; the full vitest suite is green. (No test asserts the sorter's item contents, so nothing needs updating — the integration test that names `c1-w1-lookup-vs-predict` checks week membership by `cell_id`, which is unchanged, and is DB-gated/skipped without a live stack.)

- [ ] **Step 5: Commit**

```bash
git add supabase/seed-data/course1-content.json supabase/migrations/20260715040000_seed_course1_content.sql
git commit -m "feat(week1): swap Lookup-or-Predict items to PTO + Medicaid Nava priorities

Drops the Mockingbird-page and translate items in favor of the two
high-stakes, Nava-relevant tasks the live-session facilitator key flags
as most important (fabricated PTO policy; fabricated Medicaid stats),
keeping the 2043-World-Cup item for the Experiment 2 tie-in.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Add the two "resources section" lessons

Add two `origin: custom`, `visibility: public`, `type: content` modules so the deck's four "AI Academy resources section" pointers resolve. They render in the "Resources & additional lessons" group alongside `custom-ai-support-at-nava` (`sort_order` 950), at 951 and 952.

**Files:**
- Modify: `supabase/seed-data/course1-content.json` (append two objects to the `modules` array, immediately after the `custom-ai-support-at-nava` object)
- Regenerate: `supabase/migrations/20260715040000_seed_course1_content.sql` (via the generator)

**Interfaces:**
- Consumes: nothing from Task 1.
- Produces: two module rows keyed `custom-how-claude-works-tokens` and `custom-controlling-claude-tools-permissions`. Resource B's body links to the AI Support resource by name (not by id).

- [ ] **Step 1: Add Resource A — tokens & context windows**

Insert this object into the `modules` array (after `custom-ai-support-at-nava`):

```json
{
  "cell_id": "custom-how-claude-works-tokens",
  "week": null,
  "week_sort_order": null,
  "origin": "custom",
  "visibility": "public",
  "title": "How Claude works: tokens & context windows",
  "type": "content",
  "dimension": [],
  "evidence_type": "reflection",
  "sort_order": 951,
  "body_md": "A little about what's happening under the hood when you use Claude — the same ideas the Week 1 live session covered, here for reference any time.\n\n## What a token is\n\nClaude doesn't look answers up in a database. When you send a prompt, your words are broken into small chunks called **tokens**. Claude then predicts the next most likely token, then the next, and the next — assembling its reply one chunk at a time from the patterns it learned during training. It's a very good autocomplete.\n\nThat's why the same prompt can produce different answers, and why a confident-sounding reply isn't automatically a correct one: Claude is completing a plausible pattern, not retrieving a verified fact.\n\n## The context window (working memory)\n\nEverything Claude can \"see\" for your conversation — your messages, its replies, and anything you've attached — lives in its **context window**. Think of it as working memory for that one chat. Two things to know:\n\n- **It's limited.** Picture a long document open on your screen: you can only see so much at once. As the conversation grows, the earliest material scrolls off the top — and unlike a scrollbar, Claude can't scroll back to reread it.\n- **It only knows the current conversation.** Without anything you provide, Claude doesn't remember you from yesterday or from another chat.\n\nWhen a chat gets very long, Claude may **compact** earlier context to make room. That always loses some detail, and you don't get to pick what stays.\n\n**Start a new chat when:**\n\n- Replies get forgetful or quality starts to slip.\n- You see the context filling up, or get a warning that the conversation is being compacted.\n- You're moving on to a new topic or a new chunk of work. One chat per workstream keeps conversations from getting muddled.\n\n## Token budget and cost\n\nLonger conversations don't just risk quality — they cost more, and the cost climbs steeply as the context window fills. For most people, Nava's monthly budget is plenty for everyday work, so you don't need to optimize every token. The simplest habit that helps both quality and cost is the same one above: start fresh chats at natural breakpoints instead of letting one conversation run forever.",
  "lab_config_json": null
}
```

- [ ] **Step 2: Add Resource B — tools & permissions**

Insert this object into the `modules` array, immediately after Resource A:

```json
{
  "cell_id": "custom-controlling-claude-tools-permissions",
  "week": null,
  "week_sort_order": null,
  "origin": "custom",
  "visibility": "public",
  "title": "Controlling what Claude can do: tools & permissions",
  "type": "content",
  "dimension": [],
  "evidence_type": "reflection",
  "sort_order": 952,
  "body_md": "Claude can do more than answer from what it already knows — it can use **tools** to go get information or take action. Knowing what those are, and how to control them, helps you get better results and stay in control.\n\n## Tools: how Claude gathers its own context\n\nBeyond the prompt you type, Claude can reach for tools to pull in what it needs — for example:\n\n- **Web search and fetching a page**, to get current information it wasn't trained on.\n- **Reading files** you share, so it can work from your actual documents.\n- **Connected apps and data** you've given it access to.\n\nThis is why a modern answer can feel up-to-date or specific: Claude gathered extra context first, then predicted its reply from that fuller picture — rather than relying only on what you pasted in.\n\n## What you control\n\nYou decide how much Claude can do on its own:\n\n- Some tools Claude may use **automatically**, without stopping to ask.\n- Others require your **approval** each time before Claude acts.\n\nYou can adjust which tools are available and which need a check-in from Claude's settings. If you're doing something sensitive, tightening these is a good habit; if you're doing routine research, letting Claude gather context on its own saves time.\n\nWhen in doubt about what's appropriate on a given Nava program or contract, check with your manager or project lead and Nava's AI Tool Policy — see the **AI Support at Nava** resource.",
  "lab_config_json": null
}
```

- [ ] **Step 3: Regenerate the migration**

Run: `node scripts/generate-course1-seed.mjs`
Expected: prints `Wrote …20260715040000_seed_course1_content.sql: 15 modules (12 week-assigned).` and exits 0. The count going 13 → 15 confirms both modules parsed and passed schema validation. A validation throw means a field is wrong (e.g. a `custom` module with a non-null `week`, or `body_md` containing `$md$`) — fix and re-run.

- [ ] **Step 4: Spot-check the generated SQL**

Run: `grep -c "custom-how-claude-works-tokens\|custom-controlling-claude-tools-permissions" supabase/migrations/20260715040000_seed_course1_content.sql`
Expected: `2` (one `insert` per new cell_id; they are unassigned, so they do not appear in the membership block).

- [ ] **Step 5: Lint + test**

Run: `nvm use 22 >/dev/null; npm run lint && npm test`
Expected: lint passes; vitest suite green.

- [ ] **Step 6: Commit**

```bash
git add supabase/seed-data/course1-content.json supabase/migrations/20260715040000_seed_course1_content.sql
git commit -m "feat(resources): add tokens/context-window + tools/permissions lessons

Two public custom resources in the Resources group so the Week 1 live
deck's 'AI Academy resources section' pointers (token math, context
window management, token budget, tool permissions) resolve.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: End-to-end verification

Confirm the migration applies to a real database and the new content renders in the app. This is the reviewer gate that the whole change works, not just that it generates.

**Files:** none modified (verification only).

**Interfaces:**
- Consumes: the committed JSON + regenerated SQL from Tasks 1–2.
- Produces: nothing.

- [ ] **Step 1: Apply the migration to a local stack (if Docker is available)**

Run: `npx supabase db reset`
Expected: all migrations + seed apply with no error (the seed migration is `ON CONFLICT DO NOTHING`, so it is re-runnable). If Docker Desktop isn't running, skip this step and note it was skipped.

- [ ] **Step 2: Verify the two resources are seeded**

Run: `npx supabase db reset` must precede this. Then query via Studio (`:54323`) or psql:
`select cell_id, visibility, sort_order from public.modules where cell_id like 'custom-%' order by sort_order;`
Expected: three rows — `custom-ai-support-at-nava` (950), `custom-how-claude-works-tokens` (951), `custom-controlling-claude-tools-permissions` (952), all `visibility = public`.

- [ ] **Step 3: Verify in the running app**

With the dev server running (`vite-dev` via preview_start) and signed in as the seeded demo user, load the academy and confirm: the "Resources & additional lessons" group shows both new lessons, each renders its Markdown body, and the Week 1 "Lookup or Predict?" sorter shows the six items including the PTO and Medicaid tasks. Capture a screenshot as proof.

- [ ] **Step 4: Final green check**

Run: `nvm use 22 >/dev/null; npm run lint && npm test`
Expected: lint passes; suite green. No commit (verification only) unless Step 3 surfaced a content fix.

---

## Self-Review

**Spec coverage:**
- Two public resources covering tokens / context window / token budget / tool permissions → Task 2 (Resources A + B). ✅
- Sorter gains PTO + Medicaid, drops Mockingbird + translate, keeps World Cup → Task 1. ✅
- Pipeline JSON → generator → migration, no hand-editing → every task Step "Regenerate." ✅
- `lint`/`test` green on Node 22 → Steps in every task + Task 3. ✅
- Migration re-applies idempotently → Task 3 Step 1. ✅
- Out-of-scope deck edits are explicitly the user's (Slides), not in any task — correct, no task needed. ✅

**Placeholder scan:** No TBD/TODO/"handle edge cases"; full `body_md` and full `items` JSON are inline. ✅

**Type/name consistency:** cell_ids `custom-how-claude-works-tokens` / `custom-controlling-claude-tools-permissions` and sort_orders 951/952 are identical across the spec, Task 2 objects, and Task 3 verification query. Sorter ids (`capital`, `offsite`, `pto`, `summary`, `medicaid`, `worldcup`) match the spec table. Module counts (13 → 15) are consistent across steps. ✅
