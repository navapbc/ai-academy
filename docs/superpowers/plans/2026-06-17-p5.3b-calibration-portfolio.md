# P5.3b — Calibration number + portfolio: implementation plan

Spec: `docs/superpowers/specs/2026-06-17-p5.3b-calibration-portfolio-design.md`
Branch: `feat/p5.3b-calibration-portfolio`

## Steps

1. `src/lib/learnerPortfolio.ts` — defensive parsers (`unknown` → typed | null) for
   the 2.15 / 2.8 / 2.9 / 2.11 transcripts, `buildLearnerPortfolio(rows)` (latest
   per lab id), `fetchLearnerPortfolio(userId)` (owner-RLS select of `lab_id,
   transcript, created_at`).
2. `src/lib/useLearnerPortfolio.ts` — fetch-state hook (mirror `useLearnerDetail`).
3. `src/components/progress/LearnerPortfolio.tsx` — four sub-panels + empty states;
   4D label map.
4. Wire a "Your portfolio & calibration" section into `LearnerDashboard.tsx`.
5. Tests: `learnerPortfolio.test.ts`, `LearnerPortfolio.test.tsx`, extend `e2e/16`.
6. Validate (lint/build/test/db reset/E2E) → PM review → PR → code review → merge →
   mark P5.3b done in PROJECT-PLAN.md.

## Risks

- Transcript shapes are historical/free-form → parsers must be tolerant (guard every
  field; null on mismatch). Covered by unit tests with garbage input.
- Keep `fetchLearnerDetail` (shared with staff) untouched to avoid staff regression.
