# P5.3a — Learner self-view dashboard: implementation plan

Spec: `docs/superpowers/specs/2026-06-17-p5.3a-learner-dashboard-design.md`
Branch: `feat/p5.3a-learner-dashboard`

## Steps

1. **Shared panels** — `src/components/progress/ProgressPanels.tsx`: lift
   `formatPct`, `STAGE_LABELS`, `StatCard`, `LabStatusBadge`, `ModuleProgressTable`,
   `LabSubmissionsList` out of `staff/LearnerDetail.tsx`. Refactor `LearnerDetail`
   to import them; keep DOM byte-identical (verify against its test).
2. **Summary lib** — `src/lib/learnerSelf.ts`: pure `summarizeOwnProgress(detail)`.
3. **Self-view** — `src/components/LearnerDashboard.tsx`: `useLearnerDetail(userId)`
   + `summarizeOwnProgress`; header, 4 cards, shared table + lab list, loading /
   error+retry / empty. Update `useLearnerDetail` doc comment (both callers).
4. **Wire** — `types.ts` (`View` += `'progress'`), `App.tsx` (conditional mount +
   aria-label), `Sidebar.tsx` ("My progress" entry for all users).
5. **Tests** — `learnerSelf.test.ts`, `LearnerDashboard.test.tsx`,
   `e2e/16-learner-dashboard.spec.ts`; confirm `staff/LearnerDetail.test.tsx` green.
6. **Validate** — lint/build/test, `supabase db reset`, full E2E. Then PM review →
   PR → code review → merge → mark P5.3a done in PROJECT-PLAN.md.

## Risks

- Staff `LearnerDetail` refactor must not change DOM (covered by its test suite).
- View-mount freshness: conditional mount ensures a fresh read on open; confirm the
  focus-management effect in App still behaves (it keys on `view`).
