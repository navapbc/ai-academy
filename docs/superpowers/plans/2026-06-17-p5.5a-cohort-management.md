# P5.5a — Cohort management: implementation plan

Spec: `docs/superpowers/specs/2026-06-17-p5.5a-cohort-management-design.md`
Branch: `feat/p5.5a-cohort-management` · **Human-review: PR then PAUSE for merge.**

## Steps

1. Migration `20260617000000_cohort_management.sql` — admin SELECT on
   `cohort_champions` + locked-down `cohort_changes` audit table (idempotent).
2. Edge Function `supabase/functions/admin-cohorts/`:
   - `admin-cohorts-core.ts` — `parseCohortAction` + copied cors/ratelimit/allowlist/
     domain helpers + `isUuid`/name validation.
   - `index.ts` — Deno glue mirroring `admin-set-role`: authn → rate-limit → admin
     authz → parse → per-action service_role write → best-effort `cohort_changes`
     audit → JSON.
   - `admin-cohorts-core.test.ts` — unit tests.
3. Client `src/lib/adminCohorts.ts` — `invokeAdminCohorts` + typed creators; reads +
   pure `buildCohortManagement`. `adminCohorts.test.ts` for the builder.
4. UI `src/components/staff/CohortManagement.tsx` (admin-only) + wire into
   `StaffArea` (admin-only clickable card → in-page view). `CohortManagement.test.tsx`.
5. Gated `adminCohorts.integration.test.ts` (service_role write path + client-write
   blocked + admin read).
6. Validate: lint/build/test; `supabase db reset`; `functions serve` + smoke (create/
   enroll/assign as admin; 403 for non-admin); full E2E for no-regression.
7. PM review → PR → code review → **PAUSE (AWAITING HUMAN MERGE)**.

## Risks

- Edge Function HTTP layer isn't integration-tested (matches admin-set-role); covered
  by pure unit tests + a manual served-function smoke + the human review.
- Keep the function admin-only (defence in depth: sidebar gate + RoleGuard + function
  authz). Reassign must upsert on `enrollments.unique(user_id)`, not insert.
