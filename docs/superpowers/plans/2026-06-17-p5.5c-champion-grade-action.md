# P5.5c — Champion grade action: implementation plan

Spec: `docs/superpowers/specs/2026-06-17-p5.5c-champion-grade-action-design.md`
Branch: `feat/p5.5c-champion-grade-action` · **Human-review: PR then PAUSE.**

## Steps
1. Migration `20260617010000_lab_review_columns.sql` — add `review_note`,
   `reviewed_by`, `reviewed_at` to `lab_submissions` (idempotent; no RLS change).
2. Edge Function `supabase/functions/review-grade/`:
   - `review-grade-core.ts` — `parseReviewAction` + copied cors/ratelimit/allowlist/
     domain helpers.
   - `index.ts` — authn → rate-limit → parse → service_role read submission (404) →
     authz (admin OR champion-of submission.user_id via cohort_champions⋈enrollments)
     → update status/note/reviewer/timestamp → JSON.
   - `review-grade-core.test.ts`.
3. Client `src/lib/reviewGrade.ts` — `submitReviewDecision` + approve/return creators.
4. UI: add Approve/Return (+ note) to the `ReviewQueue` detail; reload on success.
5. Tests: core unit, component (approve/return/error), gated integration.
6. Validate: lint/build/test; db reset; served-function smoke (approve/return 200;
   cross-cohort 403; learner 403); full E2E. PM review → PR → code review → **PAUSE**.

## Risks
- Replicating `is_champion_of` in the function (service_role can't use auth.uid()):
  query cohort_champions⋈enrollments for the caller + the submission's learner.
- Keep score immutable (approve/return only); the note is the only champion-authored
  text. Return requires a note (feedback) in the UI.
