# P5.5b — Review queue: implementation plan

Spec: `docs/superpowers/specs/2026-06-17-p5.5b-review-queue-design.md`
Branch: `feat/p5.5b-review-queue`

## Steps
1. `src/lib/reviewQueue.ts` — `ReviewQueueItem`, pure `buildReviewQueue` +
   `summarizeSubmission`, and `fetchReviewQueue` (status=reviewable, RLS-scoped,
   profiles name join).
2. `src/lib/useReviewQueue.ts` — fetch-state hook.
3. `src/components/staff/ReviewQueue.tsx` — list + in-page detail (submission blocks
   + `GradeResultCard`), read-only; loading/error/empty.
4. Wire `StaffArea`: Review queue *Soon* tile → live entry (champion + admin) →
   `showReviewQueue` in-page render.
5. Tests: `reviewQueue.test.ts` (builder + summarizeSubmission), `ReviewQueue.test.tsx`
   (component), `reviewQueue.integration.test.ts` (gated cohort-scoping).
6. Validate (lint/build/test/db reset/E2E) → PM review → PR → code review → merge →
   mark P5.5b done.

## Risks
- `transcript` shapes vary → `summarizeSubmission` must be defensive (guard fields,
  JSON fallback). Covered by unit tests incl. unknown shapes.
- Keep it read-only; the grade action is P5.5c (don't add write paths here).
