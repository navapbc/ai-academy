import { QuizQuestion } from '../types';

// Quizzes are reintroduced per-cell in a later task (P2.2). The old
// Crawl/Walk/Run checkpoint quizzes were tied to removed modules and are gone.
export const QUIZ_DATA: Record<string, QuizQuestion[]> = {};
